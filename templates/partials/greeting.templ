package partials

import "time"

templ Greeting() {
<div class="text-center">
  <p class="text-xl font-semibold text-gray-700 mb-2">Hello, World!</p>
  <p class="text-sm text-gray-500">Welcome to our HTMX-powered application.</p>
  <p class="text-xs text-gray-400 mt-2">Last updated: { time.Now().Format("15:04:05") }</p>
</div>
}
